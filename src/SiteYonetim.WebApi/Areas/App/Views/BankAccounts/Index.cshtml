@model List<(SiteYonetim.Domain.Entities.BankAccount Account, decimal Balance)>
@{
    ViewData["Title"] = "Banka Hesapları";
    var siteId = (Guid)ViewBag.SiteId;
}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0">Banka Hesapları</h1>
    <div class="d-flex gap-2">
        <a asp-area="App" asp-controller="BankAccounts" asp-action="Create" asp-route-siteId="@siteId" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i> Yeni Hesap</a>
        @if (Model != null && Model.Count >= 2)
        {
            <a asp-area="App" asp-controller="BankAccounts" asp-action="Transfer" asp-route-siteId="@siteId" class="btn btn-outline-primary"><i class="bi bi-arrow-left-right me-1"></i> Transfer</a>
        }
        <a asp-area="App" asp-controller="Sites" asp-action="Index" class="btn btn-outline-secondary">Site Değiştir</a>
    </div>
</div>
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}
@if (TempData["Message"] != null)
{
    <div class="alert alert-success">@TempData["Message"]</div>
}
@if (Model != null && Model.Count > 0)
{
    <div class="card border-0 shadow-sm">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Banka</th>
                        <th>Tür</th>
                        <th>Şube</th>
                        <th>Hesap No</th>
                        <th>IBAN</th>
                        <th>Güncel Bakiye</th>
                        <th width="180"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        var b = item.Account;
                        var bal = item.Balance;
                        <tr>
                            <td>
                                <strong>@b.BankName</strong>
                                @if (b.IsDefault)
                                {
                                    <span class="badge bg-primary ms-1">Varsayılan</span>
                                }
                            </td>
                            <td>
                                @if (b.AccountType == SiteYonetim.Domain.Entities.BankAccountType.Vadeli)
                                { <span class="badge bg-info">Vadeli</span> }
                                else
                                { <span class="badge bg-secondary">Vadesiz</span> }
                            </td>
                            <td>@b.BranchName</td>
                            <td>@b.AccountNumber</td>
                            <td class="small">@b.IBAN</td>
                            <td><strong class="@(bal >= 0 ? "text-success" : "text-danger")">@bal.ToString("N2") ₺</strong></td>
                            <td>
                                <a asp-area="App" asp-controller="BankAccounts" asp-action="Detail" asp-route-id="@b.Id" asp-route-siteId="@siteId" class="btn btn-sm btn-primary">Detay</a>
                                <a asp-area="App" asp-controller="BankAccounts" asp-action="Edit" asp-route-id="@b.Id" asp-route-siteId="@siteId" class="btn btn-sm btn-outline-primary">Düzenle</a>
                                <form asp-area="App" asp-controller="BankAccounts" asp-action="Delete" method="post" class="d-inline" asp-route-id="@b.Id" asp-route-siteId="@siteId" onsubmit="return confirm('Bu banka hesabını silmek istediğinize emin misiniz?');">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-outline-danger">Sil</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}
else
{
    <div class="alert alert-info">Henüz banka hesabı eklenmemiş. <a asp-area="App" asp-controller="BankAccounts" asp-action="Create" asp-route-siteId="@siteId" class="btn btn-primary btn-sm">Yeni hesap ekleyin</a> ve güncel bakiyeyi girin. Tahsilatlar ve giderler bu bakiyeyi güncelleyecektir. İstediğiniz kadar banka hesabı ekleyebilirsiniz.</div>
}
