@model IReadOnlyList<Site>
@{
    ViewData["Title"] = "Panel";
    var baseUrl = $"{Context.Request.Scheme}://{Context.Request.Host}";
}
<h1 class="h3 mb-4">Panel</h1>
<div class="row">
    <div class="col-md-4">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <h5 class="card-title text-muted"><i class="bi bi-building me-2"></i> Sitelerim</h5>
                <p class="display-6 mb-0">@(Model?.Count ?? 0)</p>
                <a asp-area="App" asp-controller="Sites" asp-action="Index" class="btn btn-sm btn-outline-primary mt-2">Siteleri Görüntüle</a>
                <p class="small text-muted mt-2 mb-0"><i class="bi bi-link-45deg me-1"></i> Destek URL: Site sakinlerine vereceğiniz adres</p>
            </div>
        </div>
    </div>
</div>
@if (Model != null && Model.Count > 0)
{
    <div class="mt-4">
        <h5>Son Siteler</h5>
        <ul class="list-group">
            @foreach (var site in Model.Take(5))
            {
                var destekUrl = $"{baseUrl}/Destek?siteId={site.Id}";
                <li class="list-group-item">
                    <div class="d-flex justify-content-between align-items-start flex-wrap gap-2">
                        <div class="flex-grow-1">
                            <strong>@site.Name</strong>
                            <div class="small text-muted mt-1">
                                <i class="bi bi-link-45deg me-1"></i> Destek URL: <a href="@destekUrl" target="_blank" class="text-break">@destekUrl</a>
                                <button type="button" class="btn btn-sm btn-outline-secondary ms-1 py-0 btn-copy-url" data-url="@destekUrl">Kopyala</button>
                            </div>
                        </div>
                        <a asp-area="App" asp-controller="Sites" asp-action="Edit" asp-route-id="@site.Id" class="btn btn-sm btn-outline-secondary">Düzenle</a>
                    </div>
                </li>
            }
        </ul>
    </div>
}
else
{
    <p class="text-muted mt-4">Henüz site eklenmemiş. <a asp-area="App" asp-controller="Sites" asp-action="Create">İlk sitenizi ekleyin</a>.</p>
}
@section Scripts {
<script>
document.querySelectorAll('.btn-copy-url').forEach(function(btn) {
    btn.addEventListener('click', function() {
        var url = this.getAttribute('data-url');
        if (url && navigator.clipboard && navigator.clipboard.writeText) {
            navigator.clipboard.writeText(url).then(function() {
                var orig = btn.textContent;
                btn.textContent = 'Kopyalandı!';
                setTimeout(function() { btn.textContent = orig; }, 1500);
            });
        }
    });
});
</script>
}
